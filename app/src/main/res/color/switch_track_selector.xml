<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state (gray track when checked) -->
    <item android:color="@color/primary_black" android:state_checked="true" />
    <!-- Default state (light gray track when unchecked) -->
    <item android:color="@android:color/white" />
</selector>
